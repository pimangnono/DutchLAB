{
  "extends": "solhint:recommended",
  "rules": {
    "avoid-throw": false,
    "avoid-suicide": "error",
    "avoid-sha3": "warn",
    "compiler-version": ["error", "^0.8.0"],
    "rules":{
      "no-console": "off"
    }
  },
  "plugins": ["prettier"]
}
